import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="General/Changelog" />

# Changelog

Обновления сгруппированы по дням, от новых к старым. Внутри дня — сводка по компонентам и разделам библиотеки.

{/* Дополнять существующую дату при повторной публикации в тот же день.
    Связанные правки объединять в итоговое изменение, не перечислять коммиты.
    Новую дату добавлять сверху только при первом обновлении за этот день. */}

## 22.09.2026

### Типографика компонентов

- Исправлено уменьшение текста на мобильных в Button, Link и ButtonLink, элементах выбора и группах, Tooltip, Tabs, Accordion, Breadcrumbs и Pagination. Размер шрифта и межстрочный интервал теперь постоянны на всех ширинах, включая вложенные компоненты.
- Мобильные стили применяются только к тексту страницы. У Accordion Large заголовок сохраняет размер H3; отступы перестраиваются как прежде.
- У Link и ButtonLink добавлен режим typography="inherit" для ссылок внутри абзаца: типографика наследуется от текста, включая мобильные размеры. По умолчанию размер ссылки постоянный.
- Добавлена браузерная проверка типографики компонентов на десктопе, планшете и мобильном экране.

### Текстовые Skeleton

- Link и ButtonLink учитывают свой размер и режим наследования. У элементов выбора подпись использует Subtitle, описание — Caption; заголовки и ошибки групп также заменяются соответствующими текстовыми скелетонами.
- У Tabs сохранены отдельные места для иконки, текста Body и Badge. В Breadcrumbs применяется Caption, в счетчике Pagination — Body. Высота строки и полоски берется из существующих вариантов Skeleton для Typography.

## 21.09.2026

### Массовое добавление компонентов

- Реализованы ButtonToggle, Link, ButtonLink, Checkbox, Radio, Switch и три группы выбора, Divider, Tooltip, Tabs, Accordion и AccordionGroup, Breadcrumbs и Pagination.
- Составные элементы используют общие контролы, Button, ButtonIcon, Icon, Badge, Skeleton и Divider. Добавлены Docs на основе описаний и чеклистов, матрицы состояний, адаптивные примеры и автотесты.
- При расхождениях с текстовыми требованиями применена геометрия и токены актуального макета: Subtitle 16/24 у опций, иконка Checkbox 20, размеры Inset у Divider 16/24, цвет disabled-стрелок.

### Textarea и Input

- Реализована Textarea: два размера, состояния, многострочный ввод, переносы и прокрутка, вертикальное изменение высоты, счетчик и Skeleton. Добавлены примеры, Docs, селекторы и автотесты.
- Label, Helper, счетчик, связи доступности и общие стили состояний вынесены в общую основу Input и Textarea. Существующие API и селекторы Input сохранены.
- В Docs зафиксирован приоритет высоты 112 px из Figma над требованием четырех видимых строк.

### ButtonFAB и ButtonIcon

- Добавлен ButtonFAB: четыре цвета, размер 64 × 64, иконка 40 × 40, состояния и Skeleton, режимы Floating и Inline. Floating сохраняет положение при прокрутке. Добавлены Docs, примеры и автотесты.
- У ButtonIcon исправлена рамка фокуса: теперь она снаружи, как в макете. Принудительные состояния в Storybook не меняются при наведении. Добавлена сравнительная матрица и браузерные проверки всех цветов и состояний.

### Button

- Уточнены типографика, порядок и размеры вложенных элементов, состояния бейджей. Добавлены независимые переключатели обеих иконок и обоих бейджей, примеры всех размеров с полным составом.

### CircularProgress и LinearProgress

- Обновлены настройки индикаторов и примеры. Для CircularProgress исправлены анимация дуги и цвета, размер по умолчанию — 24 px.
- В навигации выделены отдельные разделы для кругового и линейного прогресса.

### Typography и Layers

- Примитивы типографики сгруппированы по свойствам. Стили показаны в сравнительной таблице: строки — стили, столбцы — Desktop и Mobile. В ячейках доступны образцы, размеры и токены.
- Добавлены правила наложения элементов и семь токенов слоев: от `--layer-hidden` до `--layer-top`.

### Структура библиотеки и документация

- Настроены разделы General и группы Components. Запланированные компоненты отмечены заглушками.
- Overview, Layers, Custom Branding и Changelog оформлены отдельными страницами документации. Исправлено отображение таблицы токенов в Overview.
- В Docs готовых компонентов добавлены сведения о тестах, пробелах в покрытии и селекторах. Браузерные проверки дополнены проверками структуры, документации и образцов типографики.
- Удалены ссылки на закрытые макеты Figma и идентификаторы узлов из документации компонентов.
- Готовые компоненты и разделы атомов (цвета, размеры, тени, типографика и иконки) отмечены галочкой в навигации Storybook.
